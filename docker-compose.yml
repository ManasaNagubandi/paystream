services:
  postgres:
    image: postgres:16
    container_name: paystream-postgres
    environment:
      POSTGRES_DB: paystream
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"